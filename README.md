# This is a basic web server rolled in C

This is a basic web server rolled in C using unix syscalls

## How to install and run

Simply clone the repository locally and run *make*
Run the server with *./server*
